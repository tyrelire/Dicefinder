{% extends 'base.html.twig' %}

{% block title %}DiceFinder | Définir un mot de passe{% endblock %}

{% block body %}
<div class="flex flex-col min-h-screen">
    <div class="flex-grow flex items-center justify-center py-8 px-4  lg:px-8">
        <div class="w-full max-w-md">
            <div class="glassmorphism-container bg-white/30 backdrop-blur-md border border-white/40 rounded-lg shadow-lg p-8 mx-auto">
                <img class="mx-auto h-20 w-auto mb-5" src="{{ asset('images/logo-dicefinder.png') }}" alt="DiceFinder Logo">
                <h2 class="text-center text-2xl font-bold text-white mb-6">Définir votre mot de passe</h2>
                <p class="text-center text-sm text-gray-300 mb-6">Pour terminer la configuration de votre compte, veuillez définir un mot de passe. Vous pourrez l'utiliser pour vous reconnecter directement à l'avenir.</p>

                {{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
                    <div class="form-group">
                        {{ form_label(form.password, 'Nouveau mot de passe', {'label_attr': {'class': 'block text-gray-300 font-semibold'}}) }}
                        {{ form_widget(form.password, {'attr': {'class': 'mt-1 px-4 py-2 w-full border border-gray-600 rounded-md bg-gray-800 text-gray-200 focus:ring focus:outline-none focus:border-indigo-500'}}) }}
                        {{ form_errors(form.password) }}
                    </div>

                    <div class="form-group text-center">
                        {{ form_widget(form.save, {'attr': {'class': 'mt-4 w-full px-6 py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white font-semibold rounded-lg hover:from-blue-600 hover:to-indigo-700'}}) }}
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
